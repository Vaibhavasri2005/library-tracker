{
  "name": "library-tracker",
  "version": "1.0.0",
  "description": "Library Tracker Full-Stack Application",
  "main": "dist/server.js",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "build": "tsc && echo Build complete - listing dist contents && dir dist /b",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "watch": "tsc -w",
    "postbuild": "echo Build output location: && cd && echo dist folder exists: && if exist dist (echo YES) else (echo NO)"
  },
  "keywords": ["library", "tracker", "express", "typescript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.2",
    "@types/cors": "^2.8.13",
    "ts-node": "^10.9.1"
  }
}
